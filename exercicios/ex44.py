num = float(input('entre com o logaritmando: '))
base = float(input('entre com a base: '))
import math as m 
logaritmo = m.log(num) / m.log(base)
print ('o logaritmo de: ', num, 'na base' , base, ':' , logaritmo)
print (' ')